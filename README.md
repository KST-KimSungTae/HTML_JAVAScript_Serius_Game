# HTML_JAVAScript_Serius_Game
실시간으로 두 플레이어가 대결하며 단어를 맞추는 교육형 멀티플레이 웹 게임입니다.

**실행 방법 -> 클론 OR zip다운로드 -> 터미널에서 node server.js(단, MySQL 설치되어있어야함) -> http://localhost:20180/ 접속**

해당 프로젝트는 현재 배포가 되어있지않기때문에, 로컬환경에서 일반&시크릿모드에서 동시에 실행해야 멀티가 가능합니다.
